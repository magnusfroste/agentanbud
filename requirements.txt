fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
jinja2==3.1.4
croniter==2.0.1
# mcp 2.x reworked the low-level Server API (@server.list_tools() is gone),
# which mcp_server.py builds on. Stay on 1.x until that's ported.
mcp>=1.0.0,<2
starlette>=0.37.2,<0.39.0
