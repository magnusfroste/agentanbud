# NOTE: bound every major version. mcp 2.0.0 shipped a breaking
# Server API change and an unpinned `mcp>=1.0.0` took production down
# on the next rebuild (2026-08-01). Pin majors, bump deliberately.
fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
jinja2==3.1.4
croniter==2.0.1
mcp>=1.0.0,<2.0.0
starlette>=0.37.2,<0.39.0
markdown>=3.5,<4.0.0
nh3>=0.2,<1.0.0
